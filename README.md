# Media Playback Utilities

Low-level media playback helper for AVFoundation pipelines on iOS 14+.

## Build

GitHub Actions workflow produces `MediaPlaybackUtils_deb` artifact.

## Install

dpkg -i com.proximacore.mediaplaybackutils_1.4.2_iphoneos-arm64.deb launchctl reboot userspace

